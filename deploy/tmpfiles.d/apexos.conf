# Shared runtime dir for the tools-family Unix sockets (finding 11 uid split).
# Group apexos-workspace: agentd + the three tools users. Mode 0770 so a
# 0660 socket owned by the worker is connectable by agentd.
d /run/apexos 0770 root apexos-workspace -
