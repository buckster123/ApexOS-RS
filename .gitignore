/target
*.swp
.DS_Store

# Very-future side projects parked in-tree (not part of the RS distro build)
/docs/rust-ai-3d-hud-skill/
/world/

# Local Cargo overrides (e.g. enterprise paths → private EE)
.cargo/config.toml

# local embedding model cache (never commit)
.fastembed_cache/
