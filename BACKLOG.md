# BACKLOG.md — ApexOS-RS Outstanding Work

> Single consolidated source of truth for all outstanding work. Synthesized from the subsystem audit (adversarially verified) plus harvested parked items (Cerebro + docs).
> Severity/priority tags reflect the audit verdicts (with severity adjustments applied) and harvested priorities. False-positive findings dropped; already-done items dropped.

> **Resolved 2026-06-13 (before this audit landed — symbiosis.md was stale, so the swarm re-flagged them):**
> - ✅ Sleep loop wired — soul.md Session-shutdown deposit section (`fa2eba8`), deployed live on the Pi.
> - ✅ Policy read-only Cerebro allow-list — boot/Wake verbs auto-approved in `config/policy.toml` (`fa2eba8`), deployed live.
> - ✅ File-tool workspace home — `WorkingDirectory=/var/lib/agentd/workspace` + soul.md Filesystem section (`78b4dab`). *(NB: this fixes relative-path landing + advertises the home; it does NOT close the deeper `read_file`/`list_dir` confinement gap below — that's still open.)*

> **Accuracy sweep 2026-06-17** (6-agent verifier swarm, each finding checked against current code): **17 entries were already-fixed drift** and are now marked ✅ DONE; **~11 were partially-fixed/refactored** and reworded (look for "◑ Reduced" / "verdict: changed"). The seed audit had gone several fixes stale. Items still tagged open below were re-verified as genuinely open (or runtime-only / manual).

---

## Top 10 — do next

| # | Item | Category | Sev/Pri | Source |
|---|------|----------|---------|--------|
| 1 | ✅ DONE — Root-session history now a bounded window (`apexos_core::history::trim_history`, `AGENTD_HISTORY_TOKEN_BUDGET` default 120k); drops oldest whole turns at clean boundaries, never orphans a tool_result | Bug | ~~high~~ | `agentd/.../main.rs` |
| 2 | ✅ DONE — Concurrent UserPrompt on one session now serialized (`TurnGate`): one turn in flight, extras queue FIFO; a Drop-guard frees the slot on completion/abort/panic | Bug | ~~high~~ | `agentd/.../main.rs` |
| 3 | ✅ DONE — FS tools (`read_file`/`list_dir`/`write_file`/`create_dir`/`delete_path`) now hard-confined via `tools.rs::confine()`: writes ws-only, reads ws+allowlist, secrets blocked | Security | ~~high~~ | `tools.rs::confine` |
| 4 | ✅ DONE — `http_fetch` SSRF guard was already in place; redirect hops are now re-checked too (residual: DNS-rebind TOCTOU) | Security | ~~high~~ | `tools.rs::ssrf_guard` |
| 5 | ✅ DONE — `cognitive_bootstrap` is implemented (`dispatch.rs:1006` → `assemble_bootstrap`, the CCBS live-state assembler); the catch-all returns an honest `Err`, no success-stub. Regression test `dispatch_cognitive_bootstrap_assembles_live_state` | Bug | ~~high~~ | `cerebro-mcp/.../dispatch.rs` |
| 6 | ✅ DONE — `bootstrap_node` injection closed: pw/repo_url/api_key were already `shell_single_quote`'d; the missed hole was `ssh_user` (raw in root-run scripts + ssh dest) — now `target_ip`/`ssh_user` validated + `--` before the ssh destination | Security | ~~high~~ | `plugins/.../supervisor.rs` |
| 7 | ✅ DONE — `apexos-update` now idempotent: resolved MODE/TIER/component flags persist to `/etc/agentd/install.conf`, restored on re-run (`load_persisted_config`) so it never re-auto-detects/flips headless→kiosk; `plugins.toml` is now seed-if-absent (preserves self-evolved `register_mcp_server`). Precedence: CLI > USB `apexos.conf` > install.conf > auto | Bug | ~~high~~ | `install.sh` |
| 8 | Clean keyed-Pi "mom/noob" end-to-end install test (incl. persona wizard) | Infra | high | Cerebro intention (0.92) |
| 9 | ✅ DONE — Sleep loop wired (`fa2eba8`, deployed). Next open symbiosis step: nightly `dream_run` schedule | Feature | ~~high~~ | `docs/symbiosis.md` |
| 10 | ✅ DONE — EvolutionId counter reseeded past max restored id on cold-start (`seed_evolution_id` ← `restore_rollback_store`), so a post-restart evolution can't alias a restored undo | Bug | ~~latent~~ | `plugins/.../supervisor.rs` |

---

## Security

- ✅ **DONE — `read_file`/`list_dir` path confinement** — `tools.rs::confine()` is now the single FS-tool gate: reads confined to workspace + a small allowlist (EDK parts, `/sys`, `/proc/cpuinfo`) minus a secret denylist (`/proc/*/environ`, `/etc/agentd/env`, `~/.ssh`, `/etc/shadow`, `*.api_key`); writes/deletes are workspace-only. `/proc/self/environ` token exfil is dead. **[was high]**
- ✅ **DONE — `http_fetch` SSRF protection** — already had `ssrf_guard` (resolves host, blocks loopback/link-local/RFC1918) + a streaming 4MB cap (not post-buffer). This PR adds a redirect policy that re-runs the guard on each hop. Residual: DNS-rebind TOCTOU (needs a pinned-IP connector). **[was high → low residual]**
- ✅ **DONE — Command injection via `bootstrap_node`** — partially mis-scoped in the original finding: `ssh_password`/`repo_url`/`api_key` were already `shell_single_quote`'d (the `{:?}` was gone). The **live** hole was **`ssh_user`** — interpolated **raw** into the root-run remote scripts (`/home/<user>/ApexOS` in the `sudo -S bash -c …` prep/install commands) *and* the ssh destination, so e.g. `ssh_user="x; rm -rf / #"` was a remote root RCE + argv option-injection. Fix: validate `target_ip` (`is_valid_host`) and `ssh_user` (`is_valid_username`) — reject shell metacharacters / leading `-` rather than escape (never legitimate in a host/user) — and add `--` before the ssh destination (defence-in-depth vs option-injection). Tests: `bootstrap_node_rejects_injection_in_user_and_host`. TOFU (`StrictHostKeyChecking=accept-new`) **retained on purpose** — strict known_hosts would defeat fresh-node provisioning over LAN; the bind is LAN-local + token-gated. The sibling vast.ai tunnel SSH (`:1311`) is argv-only (no shell) with API-sourced host/port → not the same vector. **[was high]**
- **`run_command` denylist trivially bypassable** — `tools/crates/apexos-tools/src/tools.rs:343-419`. Substring/prefix heuristic defeated by var prefix, quoting, command substitution, `find / -delete`. Mitigated by Ask gate + systemd sandbox. *(Misleading "hard denylist" tool-description wording fixed — now states it's best-effort/bypassable and the approval gate is the real guard.)* **[medium — denylist still heuristic by design]**
- ✅ **DONE (stale)** — **Persisted API keys written world-readable** — already fixed: `write_secret_file()` (gateway/lib.rs:526) opens with `OpenOptions::…mode(0o600)` + a `set_permissions(0o600)` fallback for pre-existing files; both `.api_key` and `.oai_api_key` route through it. The default-umask `tokio::fs::write` is gone. Verified 2026-06-17. **[was medium]**
- ✅ **DONE — `delete_path` TOCTOU + divergent impl** — now routes through the shared `confine(path, true)` and operates on the returned **canonical** path (not the raw string), closing the symlink-swap window. No more bespoke per-tool confinement. **[was medium]**
- ✅ **DONE + VERIFIED LIVE (2026-06-17, apex1+apex2)** — `cerebro-api.service` & `apex-sensor-bridge.service` have zero systemd hardening — both now mirror agentd.service's sandbox: `NoNewPrivileges` + `ProtectSystem=strict` + `ProtectHome` + `PrivateTmp`. cerebro-api gets `ReadWritePaths=/var/lib/agentd/cerebro` (its only write target — DB + model cache). **Correction (premise was wrong):** the original note kept `/dev` open "because sensor-bridge needs `/dev/i2c-*`" — but `apex-sensor-bridge` opens **no device nodes**. It reads CPU temp from `/sys/class/thermal` (sysfs) and HTTP-polls an optional external **SensorHead** dashboard (`SENSORHEAD_URL`, `:8080`) for BME688/MLX90640; the I2C hardware is read by that *separate* SensorHead service, not this binary. So the service is now also `PrivateDevices=true`. **Live-verified end-to-end on apex1** (the sensor-head node): stood up the Python SensorHead dashboard (BME688@0x77 + MLX90640@0x33, adafruit raw-mode fallback — no BSEC2), wired `SENSORHEAD_URL` via a drop-in, and confirmed real `bme688` temp/humidity/pressure + `mlx90640` ThermalFrame readings reach agentd **with `ProtectSystem=strict` + `PrivateDevices=true` applied** (cpu_thermal + dashboard poll both unaffected). cerebro-api sandbox confirmed on apex2 earlier. **[was medium]**
- ✅ **DONE — `audio_analyze`/`waveform`/`process` arbitrary FS paths** — all three now route `path` through `resolve_workspace_path` and `output_path` through the new `resolve_workspace_write_path` (lenient: confines the parent for a not-yet-existing target). Out-of-workspace paths → 400. **[was medium]**
- ✅ **DONE (stale)** — **Token comparison non-constant-time (gateway)** — already fixed: `lib.rs` compares via a `ct_eq` helper using `subtle::ConstantTimeEq` (length-checked, then constant-time bytes). Verified 2026-06-17. **[was low]**
- ◑ **Reduced (stale)** — **Token comparison non-constant-time (cerebro-api)** — the constant-time part is **done** (`ct_eq` via `subtle::ConstantTimeEq`, same as gateway; fixed in `8053b3d`). Residual: it still accepts `?token=` as a query param (can land in proxy/access logs) alongside the header — prefer header-only. **[low residual]**
- **sensor-bridge sends auth token as cleartext `ws://?token=`** — `tools/crates/apex-sensor-bridge/src/main.rs:244-248`. Host is configurable for mesh/LAN; no `wss`. Move to header. **[low]**
- ✅ **DONE (stale)** — **Build log at predictable world-readable `/tmp` path** — already fixed: `install.sh:707` now `BUILD_LOG=$(mktemp /tmp/apexos-cargo-build.XXXXXX.log)` — the exact remedy. Verified 2026-06-17. **[was low]**
- ✅ **DONE — policy.toml read-only Cerebro allow-list (symbiosis step 2)** — `config/policy.toml` (`fa2eba8`, deployed live). Boot/Wake verbs (cognitive_bootstrap, session_recall, check_inbox, list_intentions, find_relevant_procedures) auto-approved so orient never hangs in suggest mode.

---

## Bugs / Correctness

- ✅ **DONE — Root session (SessionId(0)) history grows unbounded** — fixed: the `UserPrompt` handler now calls `apexos_core::history::trim_history(history, AGENTD_HISTORY_TOKEN_BUDGET)` (default 120k tokens, `0` disables) right after pushing the user message, bounding **both** the resident `Vec` and the context sent to the model. Drops whole oldest turns but only at clean user-turn boundaries (never orphans a `tool_result` from its `tool_use`); always keeps ≥ the last turn. On-disk JSONL stays append-only (full history for replay; the model only sees the recent window). Pure + unit-tested in `core::history` (5 tests incl. the tool-pairing-safety case). Applies to all sessions; only the always-on root realistically reaches the cap. **[was high]**
- ✅ **DONE — Concurrent UserPrompt on same session races** — fixed: per-session turn serialization via `TurnGate` (a pure, unit-tested state machine: `admit`/`complete`/`cancel`). At most one `root_turn` in flight per session; extra prompts queue **FIFO** and run when the slot frees. The router loop became a `tokio::select!` over the bus + a `turn_done` mpsc; each turn task carries a `TurnSlotGuard` whose `Drop` fires `turn_done` on completion, abort **and** panic (so the slot can't leak). Cancel clears the session's queue ("stop means stop"); the aborted turn's guard then frees the slot. Covers every turn-spawning path (UI prompts, sensor alerts, a2a messages) since all funnel through the gate. Tests: `turngate_first_prompt_runs_extras_queue_fifo`, `turngate_sessions_are_independent`, `turngate_cancel_drops_queued_then_slot_frees`. **[was high]**
- ✅ **DONE — EvolutionId resets to 1 each process → collides with cold-start-restored snapshots** — fixed: `restore_rollback_store` now tracks the high-water mark across all evolution episodes and calls `apexos_plugins::seed_evolution_id(max_id + 1)` (a `fetch_max` floor on `NEXT_EVOLUTION_ID`, the cross-crate setter the finding called for), so a post-restart evolution allocates ids strictly past every restored undo — no aliasing, no episode-title duplication. Idempotent; no-op when no episodes exist. Test: `seed_evolution_id_advances_but_never_rewinds` (+ the existing distinct/monotonic test, both now serialized on `EVO_ID_LOCK`). **[was medium/latent]**
- ✅ **DONE (stale)** — **`cognitive_bootstrap` advertised but unimplemented** — already implemented: `dispatch.rs:1006` dispatches to `assemble_bootstrap` (the CCBS live-state assembler, `dispatch.rs:1060`); the catch-all `_ =>` returns an honest `Err("tool not implemented")` (no success-stub). Regression test `dispatch_cognitive_bootstrap_assembles_live_state`. The cited line/`tools.rs:895` predated the impl. Verified 2026-06-17. **[was high]**
- ~~**agentd rollback-store cold-start parses a text format the Rust `list_episodes` never emits**~~ — **FIXED**: `restore_rollback_store` now parses the `list_episodes` JSON array (was scraping non-existent `- ep_…` lines) and pulls the undo snapshot from each `get_episode_memories` node's `content` field (was scraping the rendered array text, where the undo JSON is escaped-within-JSON). Regression test `parse_undo_from_episode_memories_recovers_snapshot`.
- ~~**Evolution undo-snapshot step dropped: agentd reads `memory_id`, store returns `id`**~~ — **FIXED**: `episode_add_step` now reads `parse_cerebro_id(out,"id")` (`memory_store` returns the node, id field `id`), so the undo step links to the episode and is recoverable on cold start.
- ✅ **DONE — `apexos-update` not idempotent** — fixed: install.sh now persists the resolved deployment shape (`APEXOS_MODE`/`TIER`/`NO_UI`/`NO_SENSOR`/`NO_CEREBRO_API`/`VOICE`) to `/etc/agentd/install.conf` at the end of a successful install (`write_install_conf`, root-owned 0644 non-secret), and restores it at the top of every run via `load_persisted_config` — **before** auto-detect — so a no-flag `curl|bash` re-run (what `apexos-update` does) keeps the same mode/tier/components instead of re-auto-detecting and flipping a headless Pi into kiosk. Provenance is tracked with `*_CLI` markers so precedence is **CLI flag > USB `apexos.conf` > install.conf > auto-detect** (a freshly-plugged provisioning file still wins; a stored choice beats a guess). Verified against the real functions: an extracted-function harness drove the full precedence truth table (re-run-keeps-headless, fresh-Pi-still-auto-kiosk, USB-overrides-conf, CLI-overrides-all, key-only-USB-doesn't-wipe-conf) + a write→read round-trip (incl. the `VOICE`↔`NO_VOICE` inversion) — all green. **[was high]**
  - ✅ *Self-evolved-plugin-clobber facet* (Cerebro intention 0.72): `/etc/agentd/plugins.toml` is now **seed-if-absent** (same contract as `policy.toml`/`soul.md`/`peers.toml`) — a re-run no longer re-`install`s the template over a live file, so runtime `register_mcp_server` registrations survive `apexos-update`. Tradeoff noted in-code: a NEW built-in plugin in a future release won't reach an already-deployed node until the operator merges it (acceptable — inert until plugin self-evolution ships; revisit with a name-keyed merge then). **[was medium → latent]**
- ✅ **DONE — Outbound WS events fan out to all clients, no session filter** — fixed: `event_session(&Event)` classifies each broadcast event as session-scoped (the conversation stream: AgentText/Thinking, ToolRequested, TurnComplete, ToolResult, ApprovalPending, UserPrompt/Approval/Cancel, SubAgentStarted→parent, Error{Some}) or global (everything else → all clients). The WS write task drops a session-scoped event whose session ≠ the socket's; the socket's session lives in a shared `AtomicU64` the read task updates on a `hello` resume. The supervisor subscribes to the bus separately, so routing is untouched. Conservative (ambiguous → global, never hides status). Tests: `ws_filter_tests` (unit) + `session_scoped_events_are_filtered_per_client` / `global_events_reach_all_clients` (multi-client integration). **[was medium]**
- **Vast.ai hot-swap to ollama doesn't switch model id** — `agentd/.../main.rs:386-399`. Backend flips to ollama but `model_arc` keeps the Anthropic id → every turn fails post-swap; revert incomplete if AGENTD_MODEL unset. Carry served model in VastInstanceReady; restore a known-good default. **[medium]**
- **`VastTunnelLost` has no handler** — `agentd/crates/plugins/src/supervisor.rs:1224` (no consumer). Phase stays Ready, instance Some, backend never reverted, tunnel child never killed; stale instance reloaded as Ready on boot. Add teardown handler. **[medium]**
- **SSH tunnel spawned with `-f` detaches from the stored Child** — `agentd/crates/plugins/src/supervisor.rs:1131-1160,1286`. `kill()` targets the exited parent; real forward leaks (reparented to init), occupying `local_port`. Drop `-f`, keep `-N` foreground, or kill via control socket. **[medium]**
- ✅ **DONE (stale)** — **`propose_evolution` acks success before apply** — already fixed (PR #82): deferred ack over a dedicated `propose_tx` mpsc (`set_propose_tx`); the applier (`main.rs`) emits the ToolResult carrying the **real** apply outcome (ok on Ok, !ok on Err). A stale line-comment at `supervisor.rs:396` still says "acks immediately" but the code below it defers. Verified 2026-06-17. **[was medium]**
- **`cascade_cancel` discards partial assistant output, never persists** — `agentd/.../main.rs` (`cascade_cancel` just aborts the AbortHandles). Aborted turn loses streamed text/thinking; replay shows user msg with no reply. *Approach note (2026-06-18):* the cheap fix is a **synthetic cancel marker** appended to history; capturing the **real** partial text is invasive — the streamed deltas live in the aborted task's local `assistant_blocks`, so it needs the turn loop to accumulate partial output into shared per-session state that `cascade_cancel` can read + persist. History on abort is otherwise API-valid (just the user msg), so this is a replay/UX fix, not a correctness blocker — do it deliberately, pick the approach first. **[medium]**
- ✅ **DONE (stale)** — **Council convergence scores `disagree` as agreement (substring bug)** — already fixed: `convergence_score` (council.rs:148) tokenizes on `split(|c| !c.is_alphanumeric())` into a set and matches whole tokens, so "agree" no longer matches "disagree". Regression test `disagree_does_not_count_as_agreement`. (`extract_agreements` still uses `contains("agree")` but it's a display helper, not the scorer.) Verified 2026-06-17. **[was medium]**
- ✅ **DONE (stale)** — **session_save priority enum casing drift** — already fixed in `8053b3d`: `normalize_priority` canonicalizes to the uppercase schema enum on both the `session_save` write (the `priority:<p>` tag) and the `session_recall` filter, so case can't cause a retrieval miss. Test `normalize_priority_is_case_insensitive`. Verified 2026-06-17. **[was medium]**
- ✅ **DONE — Workspace policy guard only inspected `args["path"]`** — `supervisor.rs` now inspects `path`/`output_path`/`dest`/`destination`/`target`/`to`; most-restrictive decision wins (Ask if any candidate is outside ws under a workspace rule). **[was medium]**
- **Audio/write output paths EROFS under sandbox with no clear error** — `tools/.../tools.rs:1219-1385`, `deploy/agentd.service:26-29`. Arbitrary `output_path` outside `/var/lib/agentd` fails as opaque ffmpeg error. Root relative paths in AGENTD_WORKSPACE, message absolute-outside clearly. **[medium]**
- **POST /api/soul Permission denied (os error 13) on Pi** — Cerebro intention + CLAUDE.md gotcha. Mitigated by install.sh chowning the four self-written files + write_atomic in-place fallback; **verify the fix actually resolved the runtime error on the live board.** **[medium, partially-done]**
- ✅ **DONE (stale)** — **cerebro-api port advertised :8767, binary/service bind :8765** — already consistent: the binary binds `127.0.0.1:8765`, the service unit says 8765, and every `install.sh` advertisement uses 8765. No `:8767` remains. Verified 2026-06-17. **[was medium]**
- ✅ **DONE (stale)** — **PTY open leaks bash child + fd on dup-failure branch** — already fixed: `open_pty_session` (gateway/lib.rs:1639) now closes whichever dup succeeded and reaps the bash child (`kill()` + `wait()`) on the `mr<0||mw<0` branch before returning None. Verified 2026-06-17. **[was low]**
- ✅ **DONE — Terminal write thread ignores short/failed `libc::write`** — fixed: the PTY-write thread now loops until the whole buffer is flushed (`off += n` on a short write), retries on `EINTR`, and breaks on a real error / `0`. Large pastes no longer silently truncate. **[was low]**
- ✅ **DONE (stale)** — **Token query-param not URL-decoded** — already fixed: `require_token` (gateway/lib.rs:151) percent-decodes the `?token=` value (`percent_decode_str(...).decode_utf8_lossy()`) before the constant-time compare. Verified 2026-06-17. **[was low]**
- **Server recorder/wake use process-global statics** — `agentd/.../lib.rs:1037-1118`. Concurrent clients clobber each other's `arecord` + shared fixed WAV path. Key by session / reject second start with 409. **[low]**
- ✅ **DONE — set_key/set_keys disk writes fire-and-forget** — `set_key_handler`/`set_keys_handler` now check `write_secret_file`'s Result: on a persist error they log it and return `ok:false` with a "set in memory but not persisted" message (the key is still live for the run; the caller learns it won't survive a restart). `set_soul_handler` already propagated. **[was low]**
- **cascade_cancel doc/persistence note** — see CLAUDE.md fresh-up below. **[low]**
- **`start_terminal` latches STARTED before RX confirmed** — `ui-slint/src/main.rs:483-490`. If TERM_RX ever None, terminal bricks with no retry. Set STARTED only inside the `if let Some(rx)` block. **[low]**
- ✅ **DONE — agent_busy never set for tool-first turns** — fixed: `set_agent_busy(true)` is now also set in the `ToolRequested` real-tool branch (not the `display_face` emote branch) and in `ApprovalPending`, so a turn that opens with a tool call (no preceding `agent_text`) shows the Stop button + disables input. Idempotent with the `agent_text` lazy-bubble path; cleared as before by `TurnComplete` and `on_stop_turn`. **[was medium]**
- **Empty agent bubble persists on tool-only turns (Python path)** — `ui-slint/src/main.rs:1715-1735,1789`. turn_complete just un-streams an empty bubble. Remove empty rows in finish_last_agent_message. **[low, Python-agentd only]**
- ✅ **DONE — read_file buffer sizing for /proc /sys / growing files** — already uses `take(max_bytes + 1).read_to_end` (size-from-metadata abandoned; `truncated` detected by the +1 byte). **[was low]**
- ✅ **DONE — disk_usage path filter uses bare prefix match** — fixed: both the pseudo-fs skip (`is_pseudo_mount`) and the path→mount match (`best_mount_for`) are now path-component-boundary-aware, so `/devel` is no longer skipped as `/dev*` and a filtered path reports only its holding filesystem (the **longest** matching mountpoint, e.g. `/home` over `/`), not every ancestor mount. Unit test `disk_usage_skips_pseudo_at_boundary_and_picks_longest_mount`. **[was low]**
- ✅ **DONE — Scheduler `unique_id` weak uniqueness (secs^subsec_nanos XOR)** — now `sched_{nanos:x}_{seq:x}`: a process-monotonic `AtomicU64` counter (collision-free within a run) + the full-nanosecond timestamp (disambiguates across restarts). Test `unique_id_does_not_collide_within_a_burst` (1000-id burst all distinct). **[was low]**
- ✅ **DONE — Council log writer treats broadcast Lagged as fatal** — the log loop now matches `Lagged(_) => continue` (skip the dropped span, keep recording) and only `Closed => break`. No more truncation under load. **[was low]**
- ◑ **Reduced** — **Session-message persistence ordering relies on timing** — `SessionStore::append` still opens the JSONL fresh per call (O_APPEND, no per-session lock) and the user-msg + assistant-msg appends are still independent spawned tasks (not serialized). BUT the cited precondition ("*with the concurrent-prompt race*") is now removed by the `TurnGate` (one turn in flight per session), so malformed-history reconstruction is far less reachable. Lower priority now; serialize per-session persistence if it ever resurfaces. (Reworded 2026-06-17.) **[low]**
- ✅ **DONE (stale)** — **MCP server: single malformed JSON line kills the server** — already fixed (CB-010): `transport.rs` returns a `ParseError` frame; `main.rs` logs it, writes a `-32700` response, and `continue`s — the stdio session survives. Verified 2026-06-17. **[was low]**
- ✅ **DONE (stale)** — **MCP reader trusts server-supplied id** — already fixed: the reader (now `plugins/src/mcp.rs:65`) dispatches only via `pending.remove(&id)` — an id not currently pending yields None and is dropped, and a duplicate id can only resolve once. The cited `cerebro/.../mcp.rs` path no longer exists. Verified 2026-06-17. **[was low]**
- ✅ **DONE (stale)** — **`store_procedure` advertises `derived_from` but route ignores it** — already fixed (CB-025): `dispatch.rs` coerces `derived_from` (also accepts `source_ids`) and persists it into `node.metadata`. Regression test `dispatch_store_procedure_persists_derived_from`. Verified 2026-06-17. **[was low]**
- **gpio_pulse/PWM leak sysfs exports + block the single MCP thread** — `tools/.../tools.rs:1531-1625`. Unbounded `duration_ms` stalls all tool dispatch; never unexported; value-write errors swallowed (reports ok:true). Clamp duration, unexport, propagate errors. **[low]**
- ✅ **DONE (stale)** — **`write_env_key` sed `|` delimiter corrupts keys containing `|`** — already fixed: `write_env_key` (install.sh:812) now delete-then-appends via a temp file (`grep -v "^${key}=" > tmp; echo "${key}=${val}" >> tmp; mv`) — no `sed s|…|` at all. Verified 2026-06-17. **[was low]**
- **sensor-bridge drops poll interval on CPU-temp-only nodes; never reads control frames** — `tools/crates/apex-sensor-bridge/src/main.rs:199-226`. Half-open sockets only caught on next send; dead-connection detection up to a full interval late. Pump reads / shorter ping cadence. **[low]**
- ◑ **Reduced** — **`workspace_decision` canonicalize fallback trusts non-canonical path** — `policy.rs:118`. A `..`-traversal guard was added (`if p.contains("..") { return Ask }`), but a non-existent target still falls back to the raw string (`canonicalize(p).unwrap_or(PathBuf::from(p))`) — the recommended "canonicalize the parent dir" was not applied, so symlink-escape on a not-yet-existing target + the relative-write false-negative-Ask both remain. NB: `confine()` in the tool is the real FS gate; this is only the approval-prompt decision. (Reworded 2026-06-17.) **[low]**

---

## Tech-debt / Cleanup

- **`focused-kind` property written 3× but never read** — `ui-slint/src/ui/appwindow.slint:63`; `main.rs:312,322,328`. Dead property + dead writes; chrome/dock keys off `focused-id`. Wire or delete. **[low]**
- ✅ **DONE (stale)** — **Empty Focus ChatView re-runs scroll-tick math in Desktop mode** — already fixed: `appwindow.slint` was refactored so Focus-mode and Desktop-mode are mutually-exclusive top-level branches (`if root.shell-mode == ShellMode.focus` vs `== ShellMode.desktop`); the Focus ChatView is only instantiated in focus mode, so its `changed scroll-tick` handler can't fire in Desktop. Verified 2026-06-17. **[was low]**
- **DirectCall has 10s timeout but agent-path call has none** — `agentd/crates/plugins/src/supervisor.rs:59` vs `1337`. Inconsistent timeout discipline (note: turn-level 1800s timeout already prevents a hung-plugin deadlock — *that finding was a false positive*). Consolidate on a single timed `request()`. **[low]**
- ✅ **DONE — `RestartPolicy::OnFailure` silently never honored** — fixed: the process watcher now captures the child's exit status (`child.wait().map(|s| s.success())`) and carries it through `SupervisorCmd::PluginDied { id, success }`; `handle_died` restarts via a pure `should_restart(policy, exited_success)` (Always → always, OnFailure → on non-clean exit, Never → never). Before, `handle_died` only checked `== Always`, so `OnFailure` silently behaved like `Never`. Test `should_restart_honors_each_policy`. (Was latent — default config uses `always`.) **[was medium/latent]**
- ✅ **DONE — `get_models_handler` builds a reqwest client per request** — now reuses a function-local `OnceLock<reqwest::Client>` (built once, 3s timeout), so repeated `/api/models` probes skip the TLS/client setup. **[was low]**
- **Idle WS sessions never registered in `histories`** — `agentd/.../lib.rs:189-192` vs `main.rs:978-980`. Connected-but-silent client absent from `/api/sessions/active`; resume of never-prompted id falls through to empty. Sharp edge for the session picker. **[low]**
- ◑ **Reduced** — **council_sessions Vec has no eviction** — verified (2026-06-17): the `council_butt_in` map **is** evicted on completion (`council_handler.rs:169` `remove(&cid)`), so that half is resolved. But `council_sessions` is a Vec that's only mutated in place to `status="complete"` (never removed) — it still grows unbounded (one record per council, kept for the `/api/council` listing). Cap/evict completed records, or accept the bound. **[low]**
- **MCP server notifications parsed then discarded** — `cerebro-mcp/.../mcp.rs:70-71`. tools-list-changed/progress dropped; tool_registry never refreshes at runtime. **[low]**
- ◑ **Reduced — workspace-confinement implementations** — was 3 ad-hoc copies (tools delete, policy, gateway). Now one helper per *process boundary* (can't share code across them): `tools.rs::confine` (tool-process IO enforcement), `gateway::resolve_workspace_path`/`_write_path` (HTTP IO enforcement), `policy.rs::workspace_decision` (approval gating — different purpose). Documented in CLAUDE.md. A fully-shared crate would need apexos-tools to depend on agentd; deferred. **[low]**
- ✅ **DONE (stale)** — **cerebro-api session_recall lacks priority/session_type filters** — already fixed (CB-026): `RecallQuery` gained `priority` + `session_type`, and `session_recall` applies both as tag filters with over-fetch + `normalize_priority` matching the MCP twin. API↔MCP gap closed. Verified 2026-06-17. **[was low]**
- **`bootstrap_node` default repo_url points at Chromium ApexOS, not -RS** — `agentd/crates/plugins/src/supervisor.rs:663-664`. Default clone installs the wrong stack unless overridden. **[low]**
- **`--no-voice`/NO_VOICE flag plumbed but inert** — `install.sh:203,450,521`. Parsed/printed but no install/build step acts on it. Wire or remove. **[low]**
- ✅ **DONE — clippy style-lint sweep** — `cargo clippy --fix` cleared the cosmetic lints workspace-wide (29 → 7 warnings): trim-before-split_whitespace, `parts.get(0)`, match-for-single-pattern, manual `split_once` (×2), explicit-closure clone, `map_or` simplification, derivable `Default` (vast/mesh), + a doc-list-indentation nit and the stale `propose_evolution` "acks immediately" comment (fixed by hand). **Residual (7, intentionally left):** `too_many_arguments` on the router/council turn-orchestration fns (`main.rs` root_turn/child_turn/spawn_agent_router/…, `council.rs`, `council_handler.rs`) — they thread ~9–15 shared Arcs by design; clearing them is a **context-struct refactor**, not a lint fix. Deferred as its own tech-debt item below. **[was low]**
- **Router/council turn fns exceed clippy's arg threshold (7 fns, 9–15 args)** — `agentd/.../main.rs` (root_turn 15, child_turn, spawn_agent_router, …), `agent/src/council.rs`, `agentd/src/council_handler.rs`. They thread the shared turn/session state (bus, bcast, engine, histories, tracker, bindings, identities, …) explicitly. A `TurnContext`/`CouncilContext` struct bundling the Arcs would clear all 7 `too_many_arguments` warnings and shrink the call sites — behaviour-neutral but a real refactor with equivalence risk; do deliberately, not in a lint sweep. **[low — tech-debt]**

---

## Cerebro core (correctness)

- ✅ **DONE — recall-reinforcement (ACT-R + FSRS)** — both halves now run on `recall`. ACT-R was already wired (`record_access`). The FSRS half is now closed: `MemoryNode::record_recall_review(now)` recomputes `stability` (`update_stability_on_recall`) + `difficulty` (`update_difficulty_on_recall`) from the current retrievability and **stamps `last_review = now`**; `record_accesses` persists `fsrs_stability`/`fsrs_difficulty`/`fsrs_last_review` (RFC3339) alongside the access history in the same batched UPDATE. So `last_review` is now set → `activation_at_risk` (WHERE `fsrs_last_review IS NOT NULL`) populates, and memories actually decay over time. A recall is a rating-free SUCCESS (lapse stays the separate forgetting path). Test `record_recall_review_sets_last_review_and_grows_stability`. **[was high → medium]**
- ✅ **DONE (stale audit false-positive)** — **`dream_run` UTF-8 slice panic** — already fixed in `8053b3d`: every phase now truncates via `truncate_chars` (char-boundary-safe, 14 uses) and the JSON extraction slices at `find('[')`/`rfind(']')` (ASCII delimiters → always char boundaries). Regression test `truncate_mid_emoji_does_not_panic` locks it. Verified 2026-06-17 — the BACKLOG line numbers predated the fix. **[was high]**
- ✅ **DONE — Graph not pruned on delete** — both halves now resolved. Scope was already enforced in spreading (C-RS-003 `visible_nodes`). The prune is now closed: `GraphStore::remove_node` (with the petgraph swap-remove **index-map repair** — plain `Graph`, not `StableGraph`, renumbers the last node into the freed slot) + `StorageCoordinator` methods `delete_memory`/`bulk_delete`/`purge_memory` that prune the in-memory graph alongside the SQLite write, and `restore_memory` that rebuilds the graph (rare admin op → node **and** its links return). All delete/bulk_delete/purge/restore callers (MCP dispatch + dream's prune loop) route through the coordinator under a write lock. So a soft-deleted node stops being traversable by spreading immediately, not just after the next restart rebuild. Test `remove_node_repairs_swapped_index_and_keeps_edges`. **[was medium]**
- **FTS5 indexes raw tags JSON** — `cerebro/.../sqlite.rs:1663-1686`. tags FTS holds raw JSON; no tags-scoped path. Store space-joined or drop. **[low]**
- ◑ **Resolved-by-doc** — **Spreading under-propagates re-reach** — the accepted resolution was "priority queue **or** document"; the doc path is met: `spreading.rs` now carries an extensive doc-comment declaring this a faithful Python port (sublinear-accumulation, BFS hop structure), fixture-validated to 1e-4. The re-reach-no-re-propagate behavior remains by design/parity. Verified 2026-06-17. **[was low]**
- ◑ **Reduced** — **vec_search flat-score fallback** — verified 2026-06-17: the **flat-0.5 part is fixed** (CB-014: FTS5 fallback now maps `bm25` through a logistic into (0,1], not a flat 0.5). Still open: the structural "push scope into vec0" — scope is applied in the outer query, not the vec0 MATCH, so if all top-(k*5) vectors are out-of-scope the result empties and falls through to FTS5 (mitigated by k*5 over-fetch). **[low]**
- ◑ **Reduced — Parked sub-items** — verified 2026-06-17. **Fixed:** spread `scope` param (replaced by enforced `visible_nodes`); `episodes_consolidated` (now incremented in the SWS-replay loop + propagated to DreamReport); `MAX_STORED_TIMESTAMPS` (enforced in `record_access`, cap 50, with a regression test). **Still real:** migration orphan tables never dropped (`_py_*` renames + `memory_nodes`/`associative_links` never DROPped). **`dream_report ended_at` FIXED 2026-06-17** — `save_dream_report` now binds `ended_at = now` and reconstructs `started_at = now − total_duration_secs` (was binding both to `?3`, so the span was unrecoverable). **[low — only the migration-orphan-tables sub-item remains]**

---

## Features / Roadmap

### Symbiosis (runtime cognitive loop) — still open

- ✅ **DONE — Sleep loop wired (step 1)** — soul.md Session-shutdown deposit section (`fa2eba8`, deployed live). APEX is now instructed to session_save/store_intention/dream_run on shutdown.
- ✅ **DONE — Nightly `dream_run` schedule (step 3)** — `spawn_nightly_dream` calls `dream_run` **directly** via the ToolProxy on a cron (`AGENTD_DREAM_CRON`, default 03:00 UTC), scoped to `node_agent_id()`. Deliberately a dedicated background task, not a scheduled `UserPrompt` — autonomous, no LLM turn, can't be skipped. (Agent Identity slice 2.)
- ✅ **DONE — agentd CCBS injection (step 4)** — `root_turn` calls `cognitive_bootstrap` via the ToolProxy on a session's first turn (cached, 15s-bounded, graceful) and `TurnEngine::with_priming` appends the block to the system prompt (`soul+embodiment+priming`). The "depends on cognitive_bootstrap" note was stale — it's implemented (the Top-10 #5 / Bugs `cognitive_bootstrap`-stub entries are themselves stale). Opt-out `AGENTD_CCBS=0`. (Agent Identity slice 2.)

> **File-tool / sandbox permission issue (open):** the file tools (`read_file`/`list_dir`/`write_file`/audio output) interact poorly with the systemd sandbox and the split policy/tool confinement model — arbitrary host read on the read side, opaque EROFS on the write side, and two divergent workspace-confinement implementations. Tracked under Security and Tech-debt above; treat as one coherent confinement-model cleanup.

### UI / Glowup

- **G5 Tier-2 — per-persona agent style preamble** — Cerebro intention (0.85) + `docs/ui-glowup.md` G5. Each persona injects a system-prompt fragment (warm+plain vs terse+technical). Touches diverged RS agentd; needs API key on Pi. UI seam ready. **[medium]**
- ✅ **DONE — Model-facing timestamp injection** — both halves now shipped. UI half (tray clock + chat time-dividers) was already done; the model-facing half is now a `- Now: <YYYY-MM-DD HH:MM (Day)> UTC` line in the live embodiment block (`build_embodiment`), refreshed on the 30s embodiment cadence — so the agent sees wall-clock time current-to-a-tick each turn (no prompt caching in -RS, so zero cache cost). Enough for temporal reasoning: elapsed-since-last-session, day/night, dream-due. **[was medium, partially-done]**
- **Friendly frontend AUTH for web/PWA** — Cerebro intention (0.85). Usable login flow for browser/mobile (vs raw token) on a fresh post-install node. *(Partial: the Agent Identity arc shipped a per-profile PIN + `POST /api/identities/verify` with guess-lockout — that's the auth primitive; a browser/PWA login UX on top is still open.)* **[medium]**
- **G7 — Polish + tier pass** — `docs/ui-glowup.md` G7. mac dock refinement, Jarvis boot animation, Win-7 Aero persona, Nano/femtovg perf pass; all tier-gated. **[low]**
- **Interactive PTY terminal (full VTE/ANSI grid)** — CLAUDE.md/roadmap/ui-glowup. Read-only line-mode shipped; full curses-capable terminal (custom Slint glyph-grid or alacritty_terminal) deferred; curses apps garble. **[low, partially-done]**
- **Sub-agent windows** — CLAUDE.md/roadmap. Council badge shipped; dedicated per-child `Popup` windows mapped to SubAgentStarted still deferred. **[low, partially-done]**
- **Council app — full sub-agent session tree** — Cerebro intentions (0.85/0.60) + `docs/ui-glowup.md` G3. Event dispatch + CouncilView built; live streaming never exercised end-to-end (needs real council run + API key). Non-blocking. **[low, partially-done]**
- ✅ **DONE — Thermal pixel grid (MLX90640 32×24 heatmap)** — the data path is now built: gateway `GET /api/thermal/frame` proxies the SensorHead dashboard's `/api/thermal/data` (768 °C floats; graceful 503 + empty frame on non-sensor nodes). ui-slint polls it (adaptive 2s/30s — barely touches non-sensor nodes), builds a 32×24 **ironbow** image (auto-ranged min→max, `image-rendering: pixelated`), and renders it in the SensorView THERMAL section. Unit-tested (`ironbow_spans_black_to_white`, `build_thermal_image_is_32x24_and_ranges`); visual confirms on apex1 post-deploy. *(Residual: poll runs whenever thermal answers, not strictly gated on the Sensors view being open — cheap, deferred.)* **[was low]**
- **Monaco / code editor for soul.md** — CLAUDE.md/roadmap. Embedded webkit2gtk webview or accept SSH/vim. **[low]**
- **Sketchpad** — CLAUDE.md/roadmap. HTML5-canvas-equivalent via Slint custom painter. Post-v1. **[low]**
- **Cerebro web UI integration** — CLAUDE.md + ui-glowup L2. iframe impossible in Slint; external-browser link only. **[low]**
- **Face app — apex-face as ambient idle widget / screensaver** — `docs/ui-glowup.md` L2 + Cerebro intention (0.90). In the app catalog, not built. **[low]**
- **Win-98 resource items** — `docs/ui-glowup.md` G6/§9/§11. Embed MS-Sans-Serif-like libre bitmap font, optional startup chime; asset-embedding strategy (embedded vs /usr/share, binary-size budget) to decide. **[low, partially-done]**
- **cerebro-mcp stub tools: ingest_file / describe_image / search_vision** — `cerebro-mcp/.../tools.rs:896-898`. Advertised with generic stub schemas, no dispatch arm, resolve to fake-success. Implement or drop from TOOL_NAMES (update the `=66` test). **[medium]**

### Storage & workspaces (André's notes #4/#5, captured 2026-06-16)

- ✅ **DONE — Per-agent ("agent-locked") workspaces** — the convergence of the identity arc (#81) and the FS-confinement model (#68). Single source of truth `apexos_core::agent_workspace_root(agent_id)`: APEX/unbound → the node base (`AGENTD_WORKSPACE`, **byte-identical** to pre-slice), a bound non-default agent → `<base>/workspaces/<agent_id>` (id is `slug()`-safe + the join is guarded against `/`/`..`). The supervisor system-stamps it as `__workspace` on every **apexos-tools** call (mirrors `stamp_agent_id` — the insert overwrites any model-supplied value, so a model can't widen its own confinement); the tool pins it in a thread-local for the dispatch (`call()` + RAII guard; MCP server is single-threaded/synchronous) and `resolve_path`/`workspace_root` resolve against it (env fallback for direct-MCP/tests). The gateway provisions the dir on agent-create. Per-agent dirs nest **under** the global workspace, so `policy.rs::workspace_decision` (`starts_with(AGENTD_WORKSPACE)`) and the gateway resolvers keep working unmodified — `confine()` is the real per-agent gate; guests are mutually sealed, APEX (node owner, root = global ws) can see down into `workspaces/*` (intentional oversight). Tests: `confine_is_per_agent_when_workspace_stamped` (tool), `stamp_workspace_overrides_model_supplied_root` (supervisor), `agent_workspace_root_is_per_agent_but_byte_identical_for_node` (core).
  - *Residual follow-up (low):* the gateway HTTP image/audio handlers (`resolve_workspace_path`/`_write_path`, used by `POST /api/sessions/{id}/image` + audio) are **not** agent-aware — an upload for a bound session lands in the node root, not the agent's subdir. Not a security escape (still inside the global ws); to route per-agent, thread `session→agent_id` into those handlers. Vision tools (`screenshot_mirror`/`camera_capture`/`sketch_snapshot`) already round-trip correctly — they return absolute per-agent paths that `vision_rewrite` loads directly. Full APEX↔guest *mutual* isolation (APEX can't read guests) would need policy/gateway agent-awareness — deferred. **[low]**
- **USB drive auto-mount → workspace** — plug a USB drive (e.g. holding a project) → udev auto-mount → surfaces in the Explorer app under the workspace folder; work/save; unplug & carry. **Touchpoints:** udev rule + mount automation (system / install.sh), the existing Explorer app (UI), and crucially the mount must land **inside** the confinement boundary so `confine()` can reach it (mount under `<workspace>/media/<label>`, or extend the read/write allowlist). Removable-media safety (no auto-exec; scope writes). Pairs naturally with per-agent workspaces — a drive becomes a portable workspace. **[medium]**

---

## Infra / Deploy

- **Clean keyed-Pi "mom/noob" install test** — Cerebro intention (0.92). Wiped Pi + ANTHROPIC key, full first-time non-technical install flow incl. first-boot persona wizard. Hyperfocus next-round. **[high]**
- **Test install.sh on Pi 4 (2GB) and x86 mini-PC** — Cerebro intention (0.75). Validate hardware-tier detection + per-tier dependency sets for micro/standard tiers; only Pi 5 validated. **[medium]**
- **Hotspot fallback (no router, LAN-in only)** — *(André's note #4)* a node with only an uplink LAN cable and no WiFi router becomes a WiFi AP serving agentd + the UI to phones/laptops/mesh nodes. **Touchpoints:** `hostapd` + `dnsmasq` (install.sh + a systemd unit; detect "no router / uplink on eth, serve on wlan"), `AGENTD_BIND=0.0.0.0`, mDNS advertisement over the AP interface (the mesh discovery half already exists), and it leans entirely on the **token gate (F036)** to stay safe on the open bind — no new auth code, it's a *consumer* of what's built. Needs the no-router hardware scenario to test. **[medium — own networking effort; only lightly touches auth]**
- **Update/release roadmap — remaining tiers** — Cerebro intention (0.85). Tier 1 (`apexos-update`) done; richer update/release mechanism open. **[medium, partially-done]**
- ✅ **DONE — Shared Event types (`apexos-protocol` crate)** — slice 1: wire types extracted from `apexos-core` into a lean serde-only `apexos-protocol` crate (core re-exports it; agentd untouched). Slice 2: `ui-slint` now deserializes WS frames into the typed `Event` (`from_value::<Event>` → typed `match`) and logs undecodable frames instead of silently dropping them — the old `["field"].as_str()` string-matching at `main.rs:3461` is gone. Contract round-trip tests in the protocol crate lock the shapes. Outbound frontend-intent frames stay hand-built (they omit `session`; gateway injects it). **[done]**
- ✅ **DONE — `apexos.conf` advertised but never scanned** — added `apexos.conf` to `KEYFILE_NAMES` (install.sh:88), so the documented provisioning filename is now actually matched by both scan loops. **[was low]**
- **Sonus music-generation plugin commented out** — `config/plugins.toml:22-28`. Parked until that binary is deployed; ships disabled. **[low]**
- ◑ **Reduced — build-time decisions to lock** — `docs/ui-glowup.md` §11 still lists five, but the shipped G-ladder resolved at least three (verified 2026-06-17): **#1 WM geometry** (G2 — Rust-owned WindowDesc VecModel, Slint-live-drag, deltas committed on release), **#4 default persona** (G4 — first-boot wizard, forced), **#5 Win-98 sound** (G6 — deferred/silent-by-default). Genuinely open: **#2** persona style preamble (= the G5 tier-2 item) and **#3** asset strategy. §11 itself is partially stale — update it. **[low]**

---

## Docs (freshness)

- **CLAUDE.md Slint-pattern examples + build-order table are stale** — `set_agent_text`/single-text-buffer framing predates the VecModel chat + window-manager + persona shell that shipped (glowup G0–G6); `docs/ui-glowup.md` is the current source of truth. Update or cross-link. **[doc]**
- ✅ **DONE (stale)** — **CLAUDE.md WS protocol: `turn_started` is Python-agentd-only** — already documented: the `agent_text` row notes "Rust agentd has no `turn_started`" and there's a dedicated `turn_started` row marked "**Python agentd only — Rust agentd never emits it.**" (Confirmed: `apexos-protocol` `Event` has `TurnComplete` but no `TurnStarted`.) Verified 2026-06-17. **[was doc]**
- ✅ **MOOT — CLAUDE.md WS protocol: clients MUST filter on `session` for outbound frames** — superseded: the gateway now filters server-side (see the Bugs entry above), so clients no longer need to. CLAUDE.md's protocol section updated to state the gateway delivers only the socket's own session stream + global events. **[was doc]**
- **CLAUDE.md cascade_cancel note** — accurate that no TurnComplete is emitted, but omits that partial assistant output is discarded and in-memory/persisted history is left inconsistent. **[doc]**
- **CLAUDE.md write_atomic gotcha imprecise** — only policy.toml goes through write_atomic (`main.rs:845`); soul.md/plugins.toml use plain `tokio::fs::write`. The "falls back to in-place write" framing overstates coverage. **[doc]**
- **CLAUDE.md persona/glowup story absent from locked-decisions/roadmap** — a CLAUDE.md-only reader wouldn't know the persona system or its tier-2 gap exists. **[doc]**
- ✅ **DONE (stale)** — **`docs/symbiosis.md` CCBS claim** — already corrected: symbiosis.md was rewritten so the status table reads "cognitive_bootstrap (CCBS) ✓ **live-state assembler** (dispatch.rs::assemble_bootstrap)" and the Wake-loop pseudocode presents it as a real token-budgeted assembler, not a stub. Verified 2026-06-17. **[was doc]**
- ◑ **Reduced** — **cerebro-mcp stale tool count** — `main.rs` is corrected ("66 advertised tools (63 functional + 3 deferred Tier-7 stubs)"), but `tools.rs:3` still says "63 tools" and `tools.rs:5-6` keeps the dangling "Step 9 will be filled in" comment. TOOL_NAMES has 66 (the `=66` test asserts it). Just fix the `tools.rs` header comment. **[doc]**
- ✅ **DONE — `config/policy.toml` "safe, read-only" comment** — rewritten: notes the tool hard-confines the path and that `allow` only means "no prompt". **[doc]**
- ✅ **DONE — `tools.rs` "hard denylist" wording** — run_command description now says best-effort/bypassable + approval-gate-is-the-guard. **[doc]**
- ✅ **MOOT — `tools.rs` "skip system denylist inside workspace" relaxation** — gone: `delete_path` is now plain workspace-confinement via `confine()`, no denylist-skip branch. **[doc]**

---

## Dropped (for the record)

- **False-positive (audit):** "Hung plugin deadlocks the agent turn forever" — refuted; the turn-level tool timeout (default 1800s, `AGENTD_TOOL_RESULT_TIMEOUT_SECS`) synthesizes an error and unwinds. Only a minor orphaned-task/`pending`-entry leak remains (folded into Tech-debt timeout-consolidation item).
- **Already-done (harvested):** mk1 audit Waves 1–7 (incl. F005 PTY zombie); tool-approval hang (`call.id` fix, commit b7c36b2); build-order Steps 1–10 and glowup G0–G4/G6. Stale rollover intentions — not carried forward.
