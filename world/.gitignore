# Rust build artifacts
/target/
**/*.rs.bk
*.pdb

# Cargo lockfile is committed for the binary workspace (world-app is an app),
# so it is intentionally NOT ignored.

# Editor / OS cruft
.DS_Store
*.swp
.idea/
.vscode/

# Local IPC socket (world-vision <-> world-app), if a UDS path lands here
*.sock

# Slint generated bindings (regenerated by build.rs)
