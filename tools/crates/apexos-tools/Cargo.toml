[package]
name = "apexos-tools"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "apexos-tools"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["blocking", "json"] }
